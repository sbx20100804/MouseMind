# MouseMind TODO

> 当前目标：完成 Alpha 0.3 Prism，使它从高级视觉原型进入可以日常使用的桌面工具阶段。

优先级说明：

- **P0**：影响稳定性、数据或核心动作正确性。
- **P1**：Alpha 0.3 必须交付的主流程。
- **P2**：明显提升体验，但不阻塞首个可用版本。
- **P3**：后续 AI、生态和商业化能力。

## P0 · 稳定性与工程基础

- [x] 使用精确进程匹配，支持 `*` 全局配置并兼容 `.exe` 后缀。
- [ ] 将鼠标钩子回调与 UI Dispatcher 显式解耦，避免在输入回调里执行耗时操作。
- [ ] 检查 `UnhookWindowsHookEx` 返回值并记录卸载失败。
- [ ] 为 `SendInput` 增加部分发送失败后的按键释放兜底。
- [ ] 为动作执行增加互斥、取消和超时，防止动作重入。
- [ ] 限制并清理动作冷却字典，避免长期运行不断积累键值。
- [ ] 保存配置前做模型验证；异常时保留旧配置和 `.bak` 备份。
- [ ] 配置加载失败时写入可见诊断日志，而不是静默回退。
- [ ] 增加全局未处理异常日志和崩溃报告文件。
- [ ] 实现单实例运行，第二次启动时激活已有窗口。
- [ ] 增加 `MouseMind.Core` 与 `MouseMind.Tests`，将匹配、解析、配置迁移移出 WPF 层。
- [ ] 为进程匹配、快捷键解析、冷却策略和配置迁移添加单元测试。

### P0 验收

- 连续运行 8 小时无崩溃、无明显内存增长。
- 所有模拟按键在失败路径下均能释放。
- 配置文件损坏后可恢复，旧配置不会丢失。
- GitHub Actions 同时执行 Release 构建和单元测试。

## P1 · 完整配置编辑器

- [ ] 新建配置时打开编辑面板，而不是写入占位数据。
- [ ] 编辑配置名称、强调色、启用状态和优先级。
- [ ] “选择当前应用”倒计时捕获前台进程。
- [ ] 支持浏览 EXE，并读取程序名称、路径和应用图标。
- [ ] 将进程规则从单个字符串升级为结构化匹配条件。
- [ ] 支持精确进程、窗口标题和全局默认三种匹配方式。
- [ ] 动作新增、编辑、复制、删除和拖动排序。
- [ ] 触发器选择：侧键 1、侧键 2、滚轮键、双击、长按。
- [ ] 快捷键录制器，直接捕获用户按下的组合键。
- [ ] 编辑冷却时间和是否保留鼠标原始行为。
- [ ] 表单验证、未保存状态和离开页面提示。
- [ ] 配置导入、导出和恢复默认值。

### P1 验收

用户无需编辑 JSON，即可完成：

```text
选择应用 → 选择侧键 → 录制快捷键 → 测试 → 保存 → 后台生效
```

## P1 · 桌面常驻体验

- [ ] 系统托盘图标和右键菜单。
- [ ] 关闭主窗口时驻留托盘。
- [ ] 双击托盘图标恢复窗口。
- [ ] 托盘中快速暂停、恢复和切换配置。
- [ ] 开机启动设置，并显示明确的当前状态。
- [ ] 动作 Toast 支持位置、持续时间和关闭选项。
- [ ] 多条 Toast 排队，连续动作不互相覆盖。
- [ ] 窗口位置、大小和最后打开页面持久化。
- [ ] 设置页所有开关接入真实配置，移除纯展示控件。

## P1 · 输入与动作能力

- [ ] 支持选择性阻止侧键原始行为。
- [ ] 同时处理 XButton Down/Up，正确识别长按和双击。
- [ ] 支持滚轮键、滚轮滚动和可用设备的横向滚轮。
- [ ] 新增打开应用动作。
- [ ] 新增打开 URL 动作。
- [ ] 新增写入剪贴板和输入文本动作。
- [ ] 新增等待、条件判断和通知动作。
- [ ] 轻量动作链：新增、删除、排序、逐步测试、失败停止。
- [ ] 动作执行历史包含耗时、目标应用和失败原因。

## P2 · Prism 视觉与交互细化

- [x] 统一 Obsidian Glass 颜色、字体、圆角和间距令牌。
- [x] 页面进入动画、输入信号轨道和动作结果 Toast。
- [x] 遵循 Windows “减少动画”偏好。
- [ ] 增加真正的空状态、加载状态、错误状态和保存成功状态。
- [ ] 配置卡片增加右键菜单和清晰的选中/悬停层级。
- [ ] 编辑抽屉采用焦点锁定，Esc 可关闭并恢复原焦点。
- [ ] 检查 100%、125%、150%、200% DPI 下的布局。
- [ ] 检查最小窗口尺寸和超长中英文文本。
- [ ] 完善键盘导航顺序、访问键和屏幕阅读器名称。
- [ ] 对所有正文与辅助文字执行 WCAG 对比度检查。
- [ ] 为成功、警告、错误增加图形区别，避免只靠颜色表达。
- [ ] 添加高质量应用图标、托盘图标和安装包图标。
- [ ] 为 README 添加真实 Prism 截图和短演示 GIF。

## P2 · 本地化

- [ ] 将 UI 文案从 XAML/C# 提取到 `.resx`。
- [ ] 支持简体中文和英文即时切换。
- [ ] 日志类型、动作名称和错误消息全部本地化。
- [ ] 根据系统语言选择首次启动语言。
- [ ] 为后续语言包预留社区贡献结构。

## P2 · 性能与质量

- [ ] 后台空闲 CPU 接近 0%。
- [ ] 侧键到动作开始执行 P95 小于 30ms。
- [ ] 冷启动到可操作控制在 1.5 秒以内。
- [ ] 常驻内存目标低于 120MB。
- [ ] 日志列表虚拟化，活动记录不直接绑定无限集合。
- [ ] 将高频日志批量写盘，避免输入回调频繁 I/O。
- [ ] 增加 8 小时稳定性测试和高频点击压力测试。
- [ ] 建立可重复的性能基准和版本对比。

## P2 · 发布工程

- [ ] 生成 Windows x64 自包含便携版。
- [ ] 创建 MSIX 或安装程序。
- [ ] 签名策略和 SmartScreen 发布说明。
- [ ] GitHub Release 自动生成 ZIP、校验和与更新日志。
- [ ] 添加 issue 模板、贡献指南和行为准则。
- [ ] 建立 `develop`/feature 分支或 Pull Request 工作流。
- [ ] 解决本地 Git 与 API 上传历史差异，恢复标准 push 流程。
- [ ] 添加 Dependabot 和基础静态分析。

## P3 · AI 文本与视觉动作

- [ ] 安全获取选中文字并恢复原剪贴板。
- [ ] 翻译、总结、润色、解释和提取待办事项。
- [ ] AI Provider 接口与连接测试。
- [ ] API 密钥使用 Windows Credential Manager 保存。
- [ ] 本地模型与 OpenAI-compatible Provider。
- [ ] 区域截图、OCR 和屏幕问答。
- [ ] 调用外部 Provider 前明确显示数据流向。
- [ ] 为敏感应用提供 AI 禁用名单。

## P3 · 手势、插件与生态

- [ ] 鼠标轨迹采样、简化和方向识别。
- [ ] 手势预览、取消区域和误触阈值。
- [ ] Prism 环形菜单。
- [ ] 插件 SDK 与隔离的插件执行环境。
- [ ] 配置包导入导出和版本兼容。
- [ ] 社区预设库与签名校验。
- [ ] 多设备识别和按设备配置。

## 推荐实施顺序

### Sprint 1：稳定底座

P0 输入线程、动作释放兜底、配置备份、单实例、测试项目。

### Sprint 2：配置闭环

应用捕获、配置编辑、快捷键录制、测试、保存。

### Sprint 3：桌面体验

托盘、开机启动、真实设置、Toast 队列和窗口状态持久化。

### Sprint 4：动作链与发布

基础动作链、活动历史、便携包、GitHub Release。

## Alpha 0.3 完成定义

- 用户可完全通过 UI 创建和修改配置。
- 程序可在托盘稳定运行并真实执行动作。
- 设置项全部有效，没有展示型开关。
- 中英文 UI 可切换。
- 单元测试、Release 构建和 8 小时稳定性测试通过。
- 提供可下载的 Windows x64 安装包或便携包。

